{
  "name": "code-chan-buddy",
  "displayName": "Code-Chan Buddy",
  "description": "Your coding companion with personality. Code-Chan lives in your sidebar, reacts to your code with funny, motivational, and sarcastic messages — celebrating wins and calling out your habits.",
  "version": "0.1.1",
  "publisher": "syedemad",
  "engines": {
    "vscode": "^1.116.0"
  },
  "categories": ["Other", "Visualization"],
  "keywords": ["clippy", "pet", "buddy", "fun", "motivational", "sarcastic", "companion", "ascii", "pixel"],
  "repository": {
    "type": "git",
    "url": "https://github.com/syedemad/code-chan"
  },
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "code-chan-activity",
          "title": "Code-Chan",
          "icon": "media/icon.svg"
        }
      ],
      "panel": [
        {
          "id": "code-chan-panel",
          "title": "Code-Chan",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "explorer": [
        {
          "type": "webview",
          "id": "code-chan.sidebar",
          "name": "Code-Chan"
        }
      ],
      "code-chan-activity": [
        {
          "type": "webview",
          "id": "code-chan.activity",
          "name": "Code-Chan"
        }
      ],
      "code-chan-panel": [
        {
          "type": "webview",
          "id": "code-chan.bottomPanel",
          "name": "Code-Chan"
        }
      ]
    },
    "commands": [
      {
        "command": "code-chan.showMessage",
        "title": "Code-Chan: Show Message"
      },
      {
        "command": "code-chan.selectCharacter",
        "title": "Select Character",
        "icon": "$(add)"
      },
      {
        "command": "code-chan.previewCharacter",
        "title": "Code-Chan: Preview Character Pack",
        "icon": "$(eye)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "code-chan.selectCharacter",
          "when": "view == code-chan.sidebar || view == code-chan.activity || view == code-chan.bottomPanel",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Code-Chan",
      "properties": {
        "code-chan.character": {
          "type": "string",
          "enum": ["clippy", "cat", "kaen", "ren", "yuki"],
          "enumDescriptions": [
            "Clippy — the classic paperclip assistant",
            "Cool Cat — a pixel cat with sunglasses",
            "Kaen — anime companion",
            "Ren — anime companion",
            "Yuki — anime companion"
          ],
          "default": "clippy",
          "description": "Which character to show in the panel"
        },
        "code-chan.messageFrequency": {
          "type": "string",
          "enum": ["low", "medium", "high"],
          "default": "medium",
          "description": "How often Code-Chan sends messages"
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "package": "vsce package",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "devDependencies": {
    "@types/vscode": "^1.116.0",
    "@vscode/vsce": "^3.9.1",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
